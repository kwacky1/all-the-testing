on: workflow_dispatch


jobs:
  funstuff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.1
      - uses: actions/setup-node@v5.0.0
        with:
          node-version: '12'
      - run: npm install
      - run: npm test
